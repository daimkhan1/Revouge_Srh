name: revouge4
description: Hyperlocal reverse logistics marketplace for fashion returns in Berlin
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.2

  # UI Components
  cupertino_icons: ^1.0.8

  # Image Picker (Added for integration)
  image_picker: ^1.0.7



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# ----------------------------------------------------------
# CRITICAL FIX: The 'flutter' section must be at the ROOT level.
# It must NOT be indented under 'dependencies'.
# ----------------------------------------------------------
flutter:
  uses-material-design: true

  assets:
    - assets/Revouge.jpg
